# Build plan

> Your work. The MVP broken into small increments. Each one is something you can build and verify on its own — no one-shotting whole features.

## Increments
<!-- Order them. Each should be small enough to build and verify in one pass. -->

1. **[increment]** — what it adds:
2. **[increment]** — what it adds:
3. **[increment]** — what it adds:

## Order rationale
<!-- Why this sequence — what has to exist before what. -->
-
